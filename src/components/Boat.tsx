import React from 'react';
import Icon from '@/components/icons/Icon';

const Boat: React.FC<SocialsProps> = () => {
  return (
    <div className="flex flex-wrap gap-4 py-6">
      <Icon name={'Boat'} />
    </div>
  );
};

export default Boat;
